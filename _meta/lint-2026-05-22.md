---
type: meta
tags: [lint]
created: 2026-05-22
---

# Lint report — 2026-05-22

## Summary

| Check | Issues found |
|-------|-------------|
| Broken / unresolvable links | 2 |
| Malformed frontmatter | 1 |
| Orphan pages | 5 |
| Missing frontmatter fields | 0 |
| Empty stubs | 0 |
| Duplicate concept candidates | 1 pair |
| Wanted pages | 0 |
| Contradictions (new) | 0 |
| Uncited pages (empty `sources:`) | 20 |
| **Total actionable issues** | **29** |

---

## Details

### 1. Broken / unresolvable wikilinks

#### 1a. `[[Reciprocal Rank Fusion (RRF)]]` — alias not registered (2 occurrences)

The file `02_Wiki/methods/Reciprocal Rank Fusion.md` has aliases `[RRF, rank fusion]`. It does **not** have `Reciprocal Rank Fusion (RRF)` as an alias. Two pages link with the parenthetical suffix, which will **not** resolve in Obsidian:

| File | Line |
|------|------|
| `02_Wiki/concepts/BM25.md` | 38 — inline link |
| `02_Wiki/concepts/BM25.md` | 60 — related pages list |

**Fix**: Either add `Reciprocal Rank Fusion (RRF)` to the aliases in `Reciprocal Rank Fusion.md`, or change the links in BM25.md to `[[Reciprocal Rank Fusion]]`.

---

### 2. Malformed frontmatter

#### 2a. Wikilink syntax inside YAML aliases — `Sparse Retrieval.md`

Line 3 of `02_Wiki/concepts/Sparse Retrieval.md`:
```yaml
aliases: [[[BM25]], TF-IDF, lexical retrieval, keyword search]
```

YAML frontmatter should contain plain text, not wikilinks. The `[[BM25]]` will not be parsed as an alias and may corrupt YAML parsing in some tools.

**Fix**: Change to `aliases: [BM25, TF-IDF, lexical retrieval, keyword search]`.

---

### 3. Orphan pages (0 inbound links from other wiki pages)

These pages exist in `02_Wiki/` but no other wiki page links to them. They will be unreachable from normal navigation.

| Page | Type | Notes |
|------|------|-------|
| `BeyondLLM` | tool | Only cited as a standalone example; no concept page links to it |
| `Federated RAG` | concept | Niche topic; not cross-linked from RAG or Dense Retrieval |
| `Instructor` | tool | No framework or pipeline page mentions it |
| `Unstructured` | tool | Not linked from Chunking, Docling, or any framework page |
| `VERA` | tool/benchmark | Not linked from evaluation pages or RAGAS |

**Suggested fixes**:
- `BeyondLLM` → link from `Retrieval-Augmented Generation.md` Tooling section
- `Federated RAG` → link from `Retrieval-Augmented Generation.md` Variants section
- `Instructor` → link from `LangChain.md`, `LlamaIndex.md`, or `Haystack.md` under structured output
- `Unstructured` → link from `Chunking.md` Implementations section
- `VERA` → link from `RAGAS.md` and `DeepEval.md` Related tools sections

---

### 4. Missing frontmatter fields

**None.** All 73 pages in `02_Wiki/` have the six required fields: `type`, `tags`, `status`, `created`, `updated`, `sources`.

---

### 5. Empty stubs (no body content)

**None.** All pages with `status: stub` have substantive body content (>10 non-empty lines beyond the frontmatter).

---

### 6. Duplicate concept candidates

#### 6a. `Faithfulness` ↔ `Hallucination in RAG`

Both pages describe the same underlying phenomenon from different angles:

- `Faithfulness` — frames it as a **metric** (score 0–1) measuring whether generated claims are grounded in context.
- `Hallucination in RAG` — frames it as the **failure mode** (what happens when Faithfulness is low).

The pages are currently complementary rather than redundant (different emphasis, different cross-links), but they risk duplicating content as both grow. The Faithfulness page should explicitly cross-reference Hallucination in RAG and vice versa to keep them coordinated.

**Recommendation**: Keep both pages but add explicit bidirectional cross-links stating their relationship (metric vs. phenomenon). Do not merge.

---

### 7. Wanted pages (linked but not existing)

No hard missing pages found — all `[[Page Name]]` links in `02_Wiki/` resolve to either an existing wiki page, a paper alias, or a web-clip source file.

**Soft wanted pages** — recurring technical terms in prose that could benefit from their own wiki pages (no link yet):

| Term | Appears in | Priority |
|------|-----------|---------|
| **SPLADE** | BM25.md, Milvus.md, Infinity.md, Chunking.md | Medium — sparse learned embedding model widely mentioned |
| **Cross-encoder** | Reranking.md, ColBERT.md, BEIR.md, VERA.md | Medium — central to Reranking but no dedicated model/concept page |
| **Bi-encoder** | Dense Retrieval.md, Reranking.md, ColBERT.md, Sentence-BERT.md | Low — adequately covered in context |
| **HNSW** | Vector Database.md, FAISS.md, Milvus.md, Qdrant.md | Low — index algorithm, adequately covered inline |

---

### 8. Contradictions

**No new contradictions detected.** The existing `_meta/contradictions.md` documents one open contradiction (C1): whether RAG augmentation universally helps generation quality. No new conflicting claims were found during this scan.

Potentially worth adding:
- DenseX performance: `DenseX.md` reports +9–12 Recall@5 gains on unsupervised models; `Chunking.md` reports "15–27% effectiveness loss vs. paragraph" for in-corpus retrieval. These are from different papers (`chen2024densex` vs `zhou2026chunktaxonomy`) and different task types (in-corpus vs. in-document) — consistent but potentially confusing to readers.

---

### 9. Uncited pages (empty `sources:` frontmatter)

20 pages have `sources: []` — they contain body content and claims but cite no source file from `01_Sources/`. Most are tool pages that were seeded with prior knowledge rather than ingested from a source.

**High priority** (pages with substantive content):

| Page | Type | Suggested source to add |
|------|------|------------------------|
| `BM25` | concept | Has `[!todo]` — Robertson & Zaragoza (2009) not yet in vault |
| `Faithfulness` | concept | No source; claims from RAGAS paper (`Es2023ragas`) |
| `RAG Triad` | concept | No source; TruLens / RAGAS papers |
| `RAPTOR` | method | No source; RAPTOR paper not yet in vault |
| `Reciprocal Rank Fusion` | method | No source; Cormack et al. (2009) not yet in vault |
| `ARES` | tool | No source; ARES paper not yet in vault |

**Lower priority** (standard tool pages without primary papers):

`Docling` (has `[!todo]`), `FAISS`, `FalkorDB`, `Haystack`, `Instructor`, `Kreuzberg`, `LangChain`, `LlamaIndex`, `MarkItDown`, `MLflow`, `Pinecone`, `TruLens`, `Unstructured`, `Weaviate`.

These are well-established tools whose documentation is on the web; adding web-clip sources via `/ingest-url` would resolve these.

---

## Recommended actions (prioritised)

1. **Quick fixes** (5 min):
   - Fix `[[Reciprocal Rank Fusion (RRF)]]` links in BM25.md (change to `[[Reciprocal Rank Fusion]]`).
   - Fix malformed alias in `Sparse Retrieval.md` (`[[[BM25]]` → `[BM25`).

2. **Cross-links** (15 min):
   - Add `[[Unstructured]]` to `Chunking.md` Implementations section.
   - Add `[[VERA]]` to `RAGAS.md` and `DeepEval.md` Related tools.
   - Add `[[BeyondLLM]]` and `[[Federated RAG]]` to `Retrieval-Augmented Generation.md`.
   - Add `[[Instructor]]` to `LangChain.md` or `LlamaIndex.md`.

3. **Source ingestion** (ongoing):
   - Ingest RAPTOR paper source (not yet in vault).
   - Add web clips for major tool homepages (FAISS, LangChain, etc.) to fill `sources:` gaps.
   - Add bidirectional links between `Faithfulness` and `Hallucination in RAG`.
