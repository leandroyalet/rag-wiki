---
type: meta
tags: [lint]
created: 2026-06-18
---

# Lint report — 2026-06-18

Scope: 78 pages in `02_Wiki/` (24 tools, 16 methods, 7 benchmarks, 27 concepts, 4 models). Run after ingesting [[sen2026grep]].

## Summary
| Check | Issues found |
|-------|-------------|
| Broken links | 15 distinct targets (87 occurrences) |
| Orphan pages | 6 |
| Missing frontmatter | 0 |
| Empty stubs | 0 |
| Duplicate candidates | 1 |
| Wanted pages | 2 |
| Contradictions | 0 new (1 standing) |
| Uncited pages | 19 |

## Details

### 1. Broken links (15 distinct targets, 87 occurrences)

**1a. Unresolved source citekeys — papers exist under descriptive filenames (10 targets, ~70 occurrences).**
The dominant issue. These notes cite papers with `[[citekey]]`, but the literature note was saved under a descriptive title instead of the citekey mandated by `CLAUDE.md` §3, so the link dangles. Each paper *does* exist in `01_Sources/papers/`:

| Citekey used | Cited from | Actual file |
|--------------|-----------|-------------|
| `[[fu2023mme]]` | benchmarks/MME.md | `MME_A_Comprehensive_Evaluation_Benchmark_for_Multimodal_Large_Language_Models.md` |
| `[[zhou2026chunktaxonomy]]` | concepts/Chunking.md, methods/DenseX.md, methods/Late Chunking.md | `Beyond_ChunkThenEmbed_..._Document_Chunking_Strategies_for_Information_Retrieval.md` |
| `[[moura2026adaptive]]` | concepts/Chunking.md, methods/Adaptive Chunking.md | `Adaptive_Chunking_Optimizing_ChunkingMethod_Selection_for_RAG.md` |
| `[[zhao2025moc]]` | concepts/Chunking.md, methods/MoC.md | `MoC_Mixtures_of_Text_Chunking_Learners_for_RetrievalAugmented_Generation_System.md` |
| `[[chiang2023llmeval]]` | concepts/LLM-as-Judge.md | `A_Closer_Look_into_Automatic_Evaluation_Using_Large_Language_Models.md` |
| `[[abeysinghe2024llmeval]]` | concepts/LLM-as-Judge.md | `The_Challenges_of_Evaluating_LLM_Applications_..._Approaches.md` |
| `[[brehme2025ragindustry]]` | concepts/RAG Failure Points.md, concepts/Retrieval-Augmented Generation.md | `RetrievalAugmented_Generation_in_Industry_..._Evaluation.md` |
| `[[wampler2025ragstack]]` | concepts/Retrieval-Augmented Generation.md | `Engineering_the_RAG_Stack_..._RetrievalAugmented_Generation_Systems.md` |
| `[[ding2024vera]]` | tools/VERA.md | `VERA_Validation_and_Evaluation_of_RetrievalAugmented_Systems.md` |
| `[[berges2024commOnt]]` | concepts/RDF.md | likely `Semantic_Web_Technology_for_Agent_Communication_Protocols.md` (verify) |

> **Recommended fix (one-time, systemic):** rename these literature notes to their citekey (and the sibling `.pdf`), *or* add the citekey as an `aliases:` entry in each note's frontmatter. Renaming is cleaner and matches `CLAUDE.md` §3; do it on a dedicated branch since it touches `01_Sources/` filenames (allowed for the `.md` notes; PDFs are immutable content but may be renamed). Not auto-fixed here.

**1b. Intra-wiki formatting errors (4 targets, 5 occurrences) — quick fixes:**
- `concepts/BM25.md` → `[[Reciprocal Rank Fusion (RRF)]]` (×2): page is named `Reciprocal Rank Fusion`. Fix to `[[Reciprocal Rank Fusion]]` or add `RRF` as an alias on that page.
- `concepts/Retrieval-Augmented Generation.md` → `[[Fusion-in-Decoder\]]`: stray escaped bracket. Fix to `[[Fusion-in-Decoder]]`.
- `tools/Milvus.md` → `[[...]]`: leftover template placeholder. Remove.
- `tools/RAGAS.md` → `[["RAG stands for Retrieval-Augmented Generation..."]]`: a quote wrapped in wikilink brackets. Should be a blockquote, not a link.

**1c. Malformed frontmatter alias (1 target):**
- `concepts/Sparse Retrieval.md` → `aliases: [[[BM25]], TF-IDF, ...]`: the `[[BM25]]` wikilink inside the alias list produces a spurious `[[[BM25]]` token and an alias/page collision (see §5). Replace with the bare string `BM25`.

**Excluded (valid, not broken):** `[[_meta/open-questions]]` (RAG.md, HyDE.md) and `[[CLAUDE]]` resolve to real files outside `02_Wiki/`.

### 2. Orphan pages (6) — zero inbound `[[links]]` from any `02_Wiki/` page
- `concepts/Federated RAG.md`
- `methods/Reciprocal Rank Fusion.md` — *would not be orphaned* if BM25.md's broken `[[Reciprocal Rank Fusion (RRF)]]` (§1b) were fixed; also linked from RAG-Fusion/Hybrid Search prose as plain text in some places.
- `tools/BeyondLLM.md`
- `tools/Instructor.md`
- `tools/Unstructured.md`
- `tools/VERA.md` — linked only via the broken `[[ding2024vera]]` citekey, not as a page.

Remediation: add a `[[link]]` from a relevant hub page (e.g. link Reciprocal Rank Fusion from Hybrid Search/RAG-Fusion bodies; link the tool pages from a comparison/related section).

### 3. Missing frontmatter (0)
All 78 pages carry the six required fields (`type`, `tags`, `status`, `created`, `updated`, `sources`).

### 4. Empty stubs (0)
77 of 78 pages carry `status: stub`, but this is the convention (`CLAUDE.md` §5: only a human promotes a page off `stub`). None have a placeholder-only body — every page has real prose. No action.

### 5. Duplicate / alias-collision candidates (1)
- **`concepts/BM25.md` (page) vs `concepts/Sparse Retrieval.md` alias `BM25`.** Sparse Retrieval lists `BM25` as an alias while BM25 has its own dedicated page, so `[[BM25]]` is ambiguous at the data level. These are *not* duplicate content (BM25 = the specific ranking function; Sparse Retrieval = the broader family), so the fix is to **remove `BM25` from Sparse Retrieval's aliases**, not to merge. (Resolving this also clears §1c.)

### 6. Wanted pages (2) — terms used across multiple pages with no page of their own
- **SPLADE** — mentioned in 6 pages (BM25, Sparse Retrieval, Hybrid Search, Sentence-BERT, Infinity, Milvus) as a learned-sparse retriever, but has no page and is never `[[linked]]`. Strong candidate for a `models/` or `concepts/` page.
- **Chronos** — mentioned in 3 pages (all from this ingest: LongMemEval, Agent Harness, Agentic Search) as the custom temporal-memory harness of [[sen2026grep]] (arXiv:2603.16862). Candidate for a `tools/` or `methods/` page if the team wants it tracked; currently intentionally inline.
- *Not wanted:* **DPR** (7 mentions) is already covered as an alias of [[Dense Retrieval]]; **ReAct** and **Context Rot** now have pages (created this session).

### 7. Contradictions (0 new)
- Standing: **C1** in `_meta/contradictions.md` (does RAG always improve generation quality) — still `open`. No new cross-page contradictions detected. The new [[sen2026grep]] claim "grep generally beats vector" is explicitly scoped to long-memory conversational QA and does not contradict the dense-retrieval pages (which it cross-references), so no entry was added.

### 8. Uncited pages (19) — no `sources:` entries and no inline source citation
Mostly tool pages that cite homepage/repo rather than `01_Sources/`, plus a few concept/method pages:
- Concepts/methods: `BM25.md`, `Faithfulness.md`, `RAG Triad.md`, `RAPTOR.md`, `Reciprocal Rank Fusion.md`
- Tools: `ARES.md`, `FAISS.md`, `FalkorDB.md`, `Haystack.md`, `Instructor.md`, `Kreuzberg.md`, `LangChain.md`, `LlamaIndex.md`, `MLflow.md`, `MarkItDown.md`, `Pinecone.md`, `TruLens.md`, `Unstructured.md`, `Weaviate.md`

Several already carry a `> [!todo] Source needed` marker (e.g. BM25, Reciprocal Rank Fusion), which is the sanctioned interim state per `CLAUDE.md` §7.6. Tool pages may legitimately cite only their homepage/repo — consider whether `sources:` should accept a homepage URL for tools, or add the relevant web clip / survey citation.

## Suggested priority
1. **Fix §1b/§1c** (5 quick edits) — eliminates the only true intra-wiki broken links.
2. **Resolve §1a** on a branch — rename literature notes to citekeys (or alias them); clears ~70 dangling citations and is the single highest-impact fix.
3. **De-orphan §2** by linking Reciprocal Rank Fusion, VERA, and the three tool pages from hub pages.
4. Create **SPLADE** (§6); decide on Chronos.
